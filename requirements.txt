# ─────────────────────────────────────────────
#  FedEx DIM Weight Predictor — Requirements
#  Python 3.10+
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Compatibility pins (do not upgrade without testing) ───────────────────────
#  setuptools 70+ dropped pkg_resources — TensorBoard depends on it
setuptools==69.5.1
#  tensorboard 2.17.0 is the last stable version compatible with the above
tensorboard==2.17.0
tensorboardX>=2.6
#  NumPy 2.0 removed np.string_ which TensorBoard still uses internally
numpy>=1.24,<2.0
# ──────────────────────────────────────────────────────────────────────────────

# Core data
pandas>=2.1
pyarrow>=15.0          # parquet read/write for preprocessed splits
openpyxl>=3.1          # read the raw FedEx .xlsx invoice export

# Machine learning — scikit-learn stack
scikit-learn>=1.4
xgboost>=2.0
optuna>=3.5            # hyperparameter tuning

# Deep learning
torch>=2.2
pytorch-lightning>=2.2
torchmetrics>=1.3      # AUC, F1, RMSE etc. inside Lightning modules

# Experiment tracking
# TensorBoard pinned above — launch with: tensorboard --logdir=logs/

# Interpretability
shap>=0.45             # SHAP beeswarm & summary plots

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Notebook support
jupyter>=1.0
ipykernel>=6.29